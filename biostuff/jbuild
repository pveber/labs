(jbuild_version 1)

(library
 ((name labs_biostuff)
  (public_name labs.biostuff)
  (libraries (biocaml.base biocaml.unix core pipes.unix))
  (preprocess (pps (ppx_compare ppx_fields_conv ppx_csv_conv)))
))
