(jbuild_version 1)

(library
 ((name labs_bistro)
  (public_name labs.bistro)
  (libraries (biocaml.base labs.biopipes bistro.bioinfo bistro.utils))
  (preprocess (pps (ppx_let ppx_sexp_conv bistro.ppx)))
))
