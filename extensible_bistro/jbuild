(jbuild_version 1)

(library
 ((name labs_extensible_bistro)
  (public_name labs.extensible_bistro)
  (libraries (core lwt.unix rresult))
  (flags (:standard -unsafe-string))
  (preprocess (pps (ppx_let)))
))
